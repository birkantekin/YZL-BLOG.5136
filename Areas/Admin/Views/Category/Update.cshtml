@using Framework.Domain.Entities;
@model Category
@{
    ViewData["Title"] = "Kategori Güncelle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="tm-contact-img-container">
</div>

<section class="tm-section">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-6 col-xl-6">

                <section>
                    <h3 class="tm-gold-text tm-form-title">Kategori Güncelle</h3>
                    <p class="tm-form-description">Aşağıda bulunan alanlara doldurarak kategori güncelleyebilirsiniz.</p>


                    <form asp-action="Update" asp-controller="Category" asp-route-area="Admin" method="post" class="tm-contact-form" enctype="multipart/form-data">
                        @Html.HiddenFor(x=>x.Id)

                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div asp-validation-summary="All" class="alert alert-danger"></div>
                        }
                        
                        <div class="form-group">
                            <input type="text" class="form-control" asp-for="Name" placeholder="Başlık" required>
                            <span asp-validation-for="Name" style="color:red; font-size:13px;"></span>
                        </div>
                        <button type="submit" class="tm-btn tm-btn-success">Kaydet</button>
                    </form>
                </section>
            </div>
        </div>
    </div>
</section>

